function exponential(number) {
  return number * number;
}

function isEven(number) {
  return number % 2 === 0;
}
for (i=5;i <= 20;i++){
  console.log("The number " + i + " is even?" + isEven(i));

}
